
# 1. 해야할 것

1. 게임산업 뉴스 읽기 
2. 개인 공부

[유튜브: 블록아웃 팁](https://youtu.be/09r1B9cVEQY?si=8_NqszKVKNoSiyrN)

[유튜브: 판타지 월드 레벨디자인](https://www.youtube.com/watch?v=YxB8AK0YY4o&t=54s)

[UE: 스플라인 예제](https://dev.epicgames.com/documentation/ko-kr/unreal-engine/blueprint-splines-content-examples?application_version=4.27&utm_source=chatgpt.com)

# 2. 오늘 배운 것
길 생성기

<details>
<summary>접기/펼치기</summary>
아, 이제 그림이 좀 더 선명해졌어 👍

* 플레이어가 **어떤 지점(트리거)** 에 들어가면
* 그 지점을 중심으로 **아래 / 좌우에서 메시들이 슉슉 날아와서 “길”이 만들어지고**
* 이 **BP 하나를 다른 위치에 복사해서 두면, 거기서도 똑같이 길이 생성**

이걸 **완전 재사용 가능한 단일 Actor BP** 구조로 짜보자.

---

## 전체 구조 개념

**BP_PathSpawner (가칭)**

구성:

1. `Box Collision` – 플레이어가 들어오는 트리거
2. `Spline Component` – 길의 **중심선** (에디터에서 점 찍어서 모양 조절) ([Epic Games Developers][1])
3. `Spline Mesh` 또는 `StaticMesh 컴포넌트들` – 실제 길을 이루는 조각들
4. `Timeline` – “아래 · 좌우에서 → 제자리로” 날아오는 애니메이션 ([Isara Tech.][2])

**핵심 아이디어**

* **길의 최종 위치는 Spline 기준** 으로 정해두고,
* **시작할 때는 아래/좌우로 오프셋된 위치** 에 두었다가
* Timeline + Lerp로 **시작 위치 → 최종 위치** 로 이동시키기 ([Epic Games Developers][3])

그래서 이 BP를 다른 위치/회전에 두더라도,
**“로컬 기준”이라서 똑같은 연출이 재사용**돼요.

---

## 1. BP_PathSpawner 만들기

1. Blueprint Class → **Actor**
2. 이름: `BP_PathSpawner`

### 컴포넌트

* `Root` (DefaultSceneRoot)
* `TriggerBox` (Box Collision)
* `PathSpline` (Spline Component)

**TriggerBox 설정**

* Collision Presets: `Trigger`
* Generate Overlap Events: 체크
* 플레이어와 Pawn 채널 Overlap

**PathSpline**

* 길이 어느 정도인지, 곡선이 필요한지 등은 에디터에서 이 BP 배치 후
  → Spline 포인트 잡고 모양을 바꿔가며 조절 (공식 스플라인 예제와 동일 패턴) ([Epic Games Developers][4])

---

## 2. 길 메시를 자동 배치 (스플라인 따라)

길을 만들 재료 메시가 있다고 치자: `SM_PathPiece` 라는 Static Mesh.

### 변수 추가

* `PathMesh` – Static Mesh 타입, Instance Editable 체크
* `SegmentLength` – float (예: 200.0)
* `bPathBuilt` – bool, 기본 false

### Construction Script 로직 (에디터에서 길 생성)

`Construction Script`에서:

1. 기존에 만든 SplineMesh 컴포넌트들 Clear

   * `GetComponentsByClass (SplineMeshComponent)`
   * ForEach → `DestroyComponent`
2. `PathSpline`의 전체 길이 `Get Spline Length`
3. `For` 루프 (i=0 → NumSegments-1)

   * StartDistance = i * SegmentLength
   * EndDistance = (i+1) * SegmentLength
   * `Add Spline Mesh Component`

     * Static Mesh = PathMesh
     * `Set Start and End`:

       * `GetLocationAtDistanceAlongSpline` (World / Local 기준 통일)
       * `GetTangentAtDistanceAlongSpline`
   * 이때, 나중에 움직이기 위해 `CreatedComponents` 배열에 저장하거나
     Tag로 구분해도 됨

이 구조는 Epic에서 소개하는 “스플라인 위에 컴포넌트 배치” 예제랑 같은 방식이야. ([Epic Games Developers][4])

> 이렇게 하면
>
> * BP를 레벨에 배치하고
> * Spline을 휘어도
>   → 길 메시가 자동으로 따라가면서 길이 만들어짐.

---

## 3. “아래 / 좌우에서 날아와서 길로 끼워지기” 연출

### 접근 방식

**재생 순간**:

* 각 SplineMesh마다

  * **현재 위치 = 최종 위치** 저장
  * **시작 위치 = 아래/좌우로 오프셋된 위치** 계산

    * 예: 아래에서 올라오게 → Z축 -값
    * 좌/우에서 슬라이드 → Spline의 Right Vector 방향 ±값 이용
  * 메시를 “시작 위치”로 먼저 옮겨놓은 후
* Timeline으로 0→1 값을 받아

  * `Lerp(시작 위치, 최종 위치)` 로 매 틱 이동

이 패턴은 공식 튜토리얼에서 Timeline+Lerp로 트랜스폼 애니메이션 만드는 방법과 똑같은 구조야. ([Epic Games Developers][3])

---

## 4. BP 내부 변수/타임라인 세팅

### 변수

* `PathMeshes` – SplineMeshComponent 배열
* `StartLocations` – Vector 배열
* `TargetLocations` – Vector 배열
* `OffsetDown` – float (예: 300.0)
* `OffsetSide` – float (예: 300.0)
* `bPlayOnce` – bool (한 번만 재생)
* `bAlreadyPlayed` – bool

### Timeline 만들기

이벤트 그래프에서:

1. 그래프 빈 곳 우클릭 → **Add Timeline**
2. 이름: `TL_PathAssemble`
3. 더블클릭해서 Timeline 에디터 열기

   * Float Track 하나 추가 (`Alpha`)
   * 0s: Value 0
   * 1~2s: Value 1 (원하는 길이)
   * Auto/Linear로 부드럽게

---

## 5. 플레이어가 들어왔을 때 로직 (On BeginOverlap)

`TriggerBox` 선택 → OnComponentBeginOverlap 이벤트 추가.

### 5-1. 필터 & 재생 조건

1. Other Actor → 캐스트 (플레이어 캐릭터) 또는 태그 체크
2. Branch: `Condition = !bPlayOnce || !bAlreadyPlayed`

   * False면 아무것도 안 함

### 5-2. 각 메시의 Start/Target 위치 세팅

Overlap 발생 시:

1. `PathMeshes` 배열 채우기

   * `GetComponentsByClass (SplineMeshComponent)` 로 받아와서 `PathMeshes`에 저장
2. `Clear` StartLocations/TargetLocations 배열
3. ForEach `PathMeshes`:

   * `TargetLoc = Mesh → GetRelativeLocation` (또는 WorldLocation, 기준 통일)
   * **오프셋 방향 계산**

     * 아래에서: `StartLoc = TargetLoc + (0, 0, -OffsetDown)`
     * 좌/우 랜덤:

       * `RightVector = GetActorRightVector`
       * `SideSign = RandomBool ? 1 : -1`
       * `StartLoc += RightVector * OffsetSide * SideSign`
   * `Mesh → SetRelativeLocation(StartLoc)` 로 실제로 내려놓기
   * `StartLocations.Add(StartLoc)`
   * `TargetLocations.Add(TargetLoc)`
4. `Play` Timeline (`TL_PathAssemble.Play`)
5. `bAlreadyPlayed = true` 로 세팅

---

## 6. Timeline 업데이트에서 길 조각 이동

`TL_PathAssemble`의 `Update` 핀에 로직 연결:

1. `Alpha` (0~1 float) 가져오기
2. For i = 0 → PathMeshes.Length - 1

   * `Mesh = PathMeshes[i]`
   * `Start = StartLocations[i]`
   * `Target = TargetLocations[i]`
   * `Curr = Lerp(Start, Target, Alpha)`
   * `Mesh → SetRelativeLocation(Curr)`

이 패턴이 “Timeline 출력 0~1 값을 Lerp Alpha로 사용하는 정석” 패턴이야. ([Epic Games Developers][3])

Timeline이 끝나면 자연스럽게 모든 메시가 최종 위치(길) 위에 딱 맞게 끼워져서 **길이 완성**됩니다.

---

## 7. 재사용성 포인트 (어디에 둬도 똑같이 동작하게)

**이 BP를 다른 곳에 두기만 하면 같은 효과**를 내려면:

1. **Spline / Mesh 위치는 전부 이 Actor의 로컬 기준(상대 좌표)로 사용**

   * `GetRelativeLocation / SetRelativeLocation` 사용
   * OffsetDown/OffsetSide도 Actor의 **로컬 축 기준**(Up/Right Vector)으로 계산
2. Actor를 월드 아무 곳에나 배치:

   * 위치/회전을 바꿔도
   * 트리거 위치 기준으로 아래/좌우에서 길이 날아오는 연출 그대로 재현
3. 인스턴스마다 다른 옵션:

   * Path Mesh 바꿔서 다른 모양의 길
   * Offset 값, Timeline 길이로 연출 변경
   * Spline 포인트만 바꿔서 곡선/직선/계단처럼 다양하게

스플라인+스플라인메시로 길을 만들고, 런타임에 스플라인 포인트나 메시를 움직여서 애니메이션 주는 방식은 공식 문서/포럼에서도 추천하는 전형적인 구조야. ([Epic Games Developers][5])

---

## 8. 더 간단한 버전 (스플라인 없이 박스 한 줄)

만약 스플라인이 너무 복잡하게 느껴지면,
**그냥 StaticMesh 여러 개를 일렬로 놓고** 똑같이 처리해도 돼.

* Components 패널에 `SM_Path_01, SM_Path_02, ...` 여러 개 추가
* Construction Script에서 배열에 넣어두고
* Overlap 시 각 메시마다 시작 위치/타겟 위치 계산 후 Timeline+Lerp 로 이동

하지만 **여러 위치마다 길 모양이 달라질 수 있다면 스플라인 방식이 훨씬 유연**해서, 나중까지 봤을 때 이쪽을 추천해.

---

## 정리

1. `BP_PathSpawner` 안에 **TriggerBox + Spline + SplineMesh들** 로 “길”의 기본 형태를 만든다.
2. 플레이어가 TriggerBox에 들어오면,

   * 각 길 조각을 아래/좌우로 오프셋한 시작 위치로 이동시키고
   * Timeline으로 시작→끝 위치를 Lerp 하면서 날아오게 만든다.
3. 이 BP를 다른 곳에 복사 배치하면,

   * Spline 모양만 살짝 수정해도
   * **똑같은 “길이 날아와서 생기는” 연출이 그 자리 기준으로 재사용**된다.

혹시 **노드 이름까지 하나하나** 적어준 예시 스샷 수준의 텍스트(노드 연결 순서)도 필요하면, “스플라인 버전으로” / “단순 메시 배열 버전으로” 둘 중 어떤 쪽이 더 좋은지 말해주면 그걸로 쫙 써줄게.

[1]: https://dev.epicgames.com/documentation/en-us/unreal-engine/blueprint-splines-in-unreal-engine?utm_source=chatgpt.com "Blueprint Splines in Unreal Engine | Unreal Engine 5.7 Documentation | Epic Developer ..."
[2]: https://isaratech.com/ue4-making-animations-with-blueprints-the-timeline/?utm_source=chatgpt.com "UE4 – Making animations with blueprints: the Timeline"
[3]: https://dev.epicgames.com/community/learning/tutorials/zwD0/unreal-engine-introduction-to-blueprints-mesh-transformations?utm_source=chatgpt.com "Introduction to Blueprints | Mesh Transformations - Epic Dev"
[4]: https://dev.epicgames.com/documentation/ko-kr/unreal-engine/blueprint-splines-content-examples?application_version=4.27&utm_source=chatgpt.com "블루프린트 스플라인 콘텐츠 예제 | 언리얼 엔진 4.27 문서 | Epic ..."
[5]: https://dev.epicgames.com/documentation/en-us/unreal-engine/blueprint-splines-content-examples?application_version=4.27&utm_source=chatgpt.com "Blueprint Splines Content Examples | Unreal Engine 4.27 Documentation | Epic Developer ..."


</details>


# 3. 개선


<details>
<summary>접기/펼치기</summary>


</details>



# 4. 생각
뭐 어떻게든 되겠지


